import { styled } from '@mui/material';

export const TopbarContainer = styled('div')(({}) => ({
  display: 'flex',
  justifyContent: 'space-between',
  minHeight: '10vh',
  alignItems: 'center',
  padding: '1rem',
}));
